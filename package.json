{
  "name": "grudge-drive",
  "version": "2.0.0",
  "description": "Velocity production entry: drive.grudge-studio.com rewrites to grudge-velocity Pages (Three.js cruise). Babylon src/ is lab-only.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev:lab": "vite",
    "build:lab": "vite build",
    "preview:lab": "vite preview",
    "build": "node -e \"console.log('No SPA build: production is CF Pages cruise. Use npm run deploy:proxy');\"",
    "deploy:proxy": "node scripts/deploy-drive-proxy.mjs",
    "deploy:pages": "node -e \"console.error('Build cruise in monorepo artifacts/arcade then wrangler pages deploy. See velocity-src/DEPLOY.md'); process.exit(1)\""
  },
  "dependencies": {
    "@babylonjs/core": "^7.54.3",
    "@babylonjs/gui": "^7.54.3",
    "@babylonjs/havok": "^1.3.12",
    "@babylonjs/loaders": "^7.54.3",
    "@babylonjs/materials": "^7.54.3"
  },
  "devDependencies": {
    "vite": "^8.0.12"
  }
}
